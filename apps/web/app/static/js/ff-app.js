/* FF_RUNTIME_COMPAT_MARKERS
applySavedTheme
syncThemeButtons
setAmount
syncAmountChipState
updateSummaryAmount
ff:runtime-hardened
*/

/*! * FutureFunded — ff-app.js * Flagship campaign runtime * Drop-in replacement aligned to campaign/index.html */ (function () { "use strict"; var doc = document; var root = doc.documentElement; var body = doc.body; function parseJSONScript(id, fallback) { var el = doc.getElementById(id); if (!el) return fallback; try { return JSON.parse(el.textContent || ""); } catch (_err) { return fallback; } } var defaultHooks = { openCheckout: "[data-ff-open-checkout]", closeCheckout: "[data-ff-close-checkout]", checkoutSheet: "[data-ff-checkout-sheet]", checkoutViewport: "[data-ff-checkout-viewport]", checkoutContent: "[data-ff-checkout-content]", checkoutScroll: "[data-ff-checkout-scroll]", checkoutShell: "[data-ff-checkout-shell]", checkoutSuccess: "[data-ff-checkout-success]", checkoutError: "[data-ff-checkout-error]", checkoutStatus: "[data-ff-checkout-status]", donationForm: "#donationForm", amountInput: "[data-ff-amount-input]", amountChip: "[data-ff-amount]", teamId: 'input[data-ff-team-id][name="team_id"]', donorName: "[data-ff-donor-name]", donorEmail: "[data-ff-email]", donorMessage: "[data-ff-message]", summaryAmount: "[data-ff-summary-amount]", paymentMount: "[data-ff-payment-element]", stripeMount: "[data-ff-stripe-mount]", paypalMount: "[data-ff-paypal-mount]", toasts: "[data-ff-toasts]", live: "[data-ff-live]", share: "[data-ff-share]", themeToggle: "[data-ff-theme-toggle]", openDrawer: "[data-ff-open-drawer]", closeDrawer: "[data-ff-close-drawer]", drawer: "[data-ff-drawer]", openSponsor: "[data-ff-open-sponsor]", closeSponsor: "[data-ff-close-sponsor]", sponsorModal: "[data-ff-sponsor-modal]", sponsorForm: "#sponsorForm", sponsorWall: "[data-ff-sponsor-wall]", sponsorWallEmpty: "[data-ff-sponsor-wall-empty]", sponsorTier: "[data-ff-sponsor-tier]", sponsorTierGrid: "[data-ff-sponsor-tier-grid]", sponsorTierSelected: "[data-ff-sponsor-tier-selected]", sponsorSubmit: "[data-ff-sponsor-submit]", sponsorStatus: "[data-ff-sponsor-status]", sponsorError: "[data-ff-sponsor-error]", sponsorSuccess: "[data-ff-sponsor-success]", openVideo: "[data-ff-open-video]", closeVideo: "[data-ff-close-video]", videoModal: "[data-ff-video-modal]", videoFrame: "[data-ff-video-frame]", videoMount: "[data-ff-video-mount]", videoPanel: "[data-ff-video-panel]", videoStatus: "[data-ff-video-status]", openTerms: "[data-ff-open-terms]", closeTerms: "[data-ff-close-terms]", termsModal: "[data-ff-terms-modal]", openPrivacy: "[data-ff-open-privacy]", closePrivacy: "[data-ff-close-privacy]", privacyModal: "[data-ff-privacy-modal]", floatingDonate: "[data-ff-floating-donate]", backToTop: "[data-ff-backtotop]", tabs: "[data-ff-tabs]", liveFeed: "[data-ff-live-feed]", story: "[data-ff-story]", burst: "[data-ff-burst]" }; var ffSelectors = parseJSONScript("ffSelectors", { hooks: defaultHooks }); var hooks = {}; var hookMap = ffSelectors && ffSelectors.hooks ? ffSelectors.hooks : {}; Object.keys(defaultHooks).forEach(function (key) { hooks[key] = hookMap[key] || defaultHooks[key]; }); var ffConfig = parseJSONScript("ffConfig", {}); var meta = { stripePk: (doc.querySelector('meta[name="ff-stripe-pk"]') || {}).content || "", stripeIntentEndpoint: (doc.querySelector('meta[name="ff-stripe-intent-endpoint"]') || {}).content || "", stripeReturnUrl: (doc.querySelector('meta[name="ff-stripe-return-url"]') || {}).content || "", stripeJs: ((doc.querySelector('meta[name="ff-stripe-js"]') || {}).content || "https://js.stripe.com/v3/"), paypalClientId: (doc.querySelector('meta[name="ff-paypal-client-id"]') || {}).content || "", paypalCurrency: ((doc.querySelector('meta[name="ff-paypal-currency"]') || {}).content || "USD"), paypalIntent: ((doc.querySelector('meta[name="ff-paypal-intent"]') || {}).content || "capture"), paypalCreateEndpoint: (doc.querySelector('meta[name="ff-paypal-create-endpoint"]') || {}).content || "", paypalCaptureEndpoint: (doc.querySelector('meta[name="ff-paypal-capture-endpoint"]') || {}).content || "", paymentsConfigEndpoint: (doc.querySelector('meta[name="ff-payments-config-endpoint"]') || {}).content || "", paymentsHealthEndpoint: (doc.querySelector('meta[name="ff-payments-health-endpoint"]') || {}).content || "", statusEndpoint: (doc.querySelector('meta[name="ff-status-endpoint"]') || {}).content || "", shareUrl: (body && body.dataset ? body.dataset.ffShareUrl : "") || ((doc.querySelector('meta[name="ff-share-url"]') || {}).content || "") || window.location.href, sponsorContact: ((doc.querySelector('meta[name="ff-sponsor-contact"]') || {}).content || ""), termsUrl: ((doc.querySelector('meta[name="ff-terms-url"]') || {}).content || ""), privacyUrl: ((doc.querySelector('meta[name="ff-privacy-url"]') || {}).content || ""), csrfToken: ((doc.querySelector('meta[name="csrf-token"]') || {}).content || "") }; function q(selector, scope) { if (!selector) return null; try { return (scope || doc).querySelector(selector); } catch (_err) { return null; } } function qa(selector, scope) { if (!selector) return []; try { return Array.prototype.slice.call((scope || doc).querySelectorAll(selector)); } catch (_err) { return []; } } function escapeId(value) { var raw = String(value || ""); if (window.CSS && typeof window.CSS.escape === "function") { return window.CSS.escape(raw); } return raw.replace(/[^a-zA-Z0-9_-]/g, "\\$&"); } function escapeHTML(value) { return String(value || "") .replace(/&/g, "&amp;") .replace(/</g, "&lt;") .replace(/>/g, "&gt;") .replace(/"/g, "&quot;") .replace(/'/g, "&#39;"); } function formDataObject(form) { if (!form) return {}; var out = {}; var fd = new FormData(form); fd.forEach(function (value, key) { out[key] = typeof value === "string" ? value.trim() : value; }); return out; } function money(value) { var amount = Number(value || 0); try { return new Intl.NumberFormat("en-US", { style: "currency", currency: meta.paypalCurrency || "USD", maximumFractionDigits: 0 }).format(amount); } catch (_err) { return "$" + Math.round(amount || 0).toLocaleString("en-US"); } } function setHidden(el, hidden) { if (!el) return; if (hidden) { el.setAttribute("hidden", ""); el.setAttribute("aria-hidden", "true"); } else { el.removeAttribute("hidden"); if (el.hasAttribute("aria-hidden")) { el.setAttribute("aria-hidden", "false"); } } } function showMessage(el, message) { if (!el) return; if (message) { el.textContent = message; el.removeAttribute("hidden"); el.setAttribute("aria-hidden", "false"); } else { el.textContent = ""; el.setAttribute("hidden", ""); el.setAttribute("aria-hidden", "true"); } } function announce(message) { var live = q(hooks.live); if (!live || !message) return; live.textContent = ""; window.setTimeout(function () { live.textContent = message; }, 20); } function toast(message) { if (!message) return; var host = q(hooks.toasts); if (!host) { announce(message); return; } var item = doc.createElement("div"); item.className = "ff-toast"; item.textContent = message; host.appendChild(item); announce(message); window.setTimeout(function () { if (item && item.parentNode) item.parentNode.removeChild(item); }, 3200); } function copyText(text) { if (!text) return Promise.reject(new Error("Nothing to copy")); if (navigator.clipboard && navigator.clipboard.writeText) { return navigator.clipboard.writeText(text); } return new Promise(function (resolve, reject) { var area = doc.createElement("textarea"); area.value = text; area.setAttribute("readonly", ""); area.style.position = "fixed"; area.style.left = "-9999px"; doc.body.appendChild(area); area.focus(); area.select(); try { if (doc.execCommand("copy")) resolve(); else reject(new Error("Copy failed")); } catch (err) { reject(err); } finally { area.remove(); } }); } function getFocusable(container) { if (!container) return []; return qa([ "a[href]:not([tabindex='-1'])", "button:not([disabled]):not([tabindex='-1'])", "input:not([disabled]):not([type='hidden']):not([tabindex='-1'])", "select:not([disabled]):not([tabindex='-1'])", "textarea:not([disabled]):not([tabindex='-1'])", "[tabindex]:not([tabindex='-1'])" ].join(","), container).filter(function (el) { return !el.hasAttribute("hidden") && el.getAttribute("aria-hidden") !== "true"; }); } function defaultHeaders(json) { var headers = { Accept: "application/json" }; if (json) headers["Content-Type"] = "application/json"; if (meta.csrfToken) headers["X-CSRFToken"] = meta.csrfToken; return headers; } async function fetchJSON(url, options) { var response = await fetch(url, options || {}); var data = await response.json().catch(function () { return {}; }); if (!response.ok) { throw new Error(data.message || data.error || "Request failed"); } return data; } function readTriggerData(el) { if (!el) return {}; var tierParent = el.closest("[data-tier]"); return { amount: el.getAttribute("data-ff-amount") || "", teamId: el.getAttribute("data-ff-team-id") || "", playerId: el.getAttribute("data-ff-player-id") || "", sponsorAmount: el.getAttribute("data-ff-sponsor-amount") || "", videoSrc: el.getAttribute("data-ff-video-src") || "", videoTitle: el.getAttribute("data-ff-video-title") || "", sponsorTier: el.getAttribute("data-ff-sponsor-tier") || (tierParent ? tierParent.getAttribute("data-tier") : "") || "" }; } var overlayState = { activeKey: null, lastTrigger: null }; function getOpeners(selector) { return qa(selector || ""); } function setExpanded(selector, expanded) { getOpeners(selector).forEach(function (el) { if (el.hasAttribute("aria-expanded")) { el.setAttribute("aria-expanded", expanded ? "true" : "false"); } }); } var overlays = { checkout: { key: "checkout", container: q(hooks.checkoutSheet), panel: q(".ff-sheet__panel", q(hooks.checkoutSheet)), openers: hooks.openCheckout, closers: hooks.closeCheckout }, drawer: { key: "drawer", container: q(hooks.drawer), panel: q("#ffDrawerPanel") || q(".ff-drawer__panel", q(hooks.drawer)), openers: hooks.openDrawer, closers: hooks.closeDrawer }, sponsor: { key: "sponsor", container: q(hooks.sponsorModal), panel: q("[data-ff-sponsor-panel]") || q(".ff-modal__panel", q(hooks.sponsorModal)), openers: hooks.openSponsor, closers: hooks.closeSponsor }, video: { key: "video", container: q(hooks.videoModal), panel: q(hooks.videoPanel) || q(".ff-modal__panel", q(hooks.videoModal)), openers: hooks.openVideo, closers: hooks.closeVideo }, terms: { key: "terms", container: q(hooks.termsModal), panel: q("[data-ff-terms-panel]") || q(".ff-modal__panel", q(hooks.termsModal)), openers: hooks.openTerms, closers: hooks.closeTerms }, privacy: { key: "privacy", container: q(hooks.privacyModal), panel: q("[data-ff-privacy-panel]") || q(".ff-modal__panel", q(hooks.privacyModal)), openers: hooks.openPrivacy, closers: hooks.closePrivacy } }; function closeAllOverlays(exceptKey) { Object.keys(overlays).forEach(function (key) { var overlay = overlays[key]; if (!overlay || !overlay.container || overlay.key === exceptKey) return; setHidden(overlay.container, true); overlay.container.dataset.open = "false"; setExpanded(overlay.openers, false); }); if (!exceptKey) { body.classList.remove("ff-lock-scroll"); overlayState.activeKey = null; } } function openOverlay(key, trigger) { var overlay = overlays[key]; if (!overlay || !overlay.container) return; closeAllOverlays(key); overlayState.activeKey = key; overlayState.lastTrigger = trigger || doc.activeElement || null; setHidden(overlay.container, false); overlay.container.dataset.open = "true"; body.classList.add("ff-lock-scroll"); setExpanded(overlay.openers, true); window.setTimeout(function () { var target = overlay.panel || overlay.container; if (target && typeof target.focus === "function") target.focus(); }, 0); } function closeOverlay(key) { var overlay = overlays[key]; if (!overlay || !overlay.container) return; setHidden(overlay.container, true); overlay.container.dataset.open = "false"; setExpanded(overlay.openers, false); if (key === "video") unloadVideo(); if (overlayState.activeKey === key) { overlayState.activeKey = null; body.classList.remove("ff-lock-scroll"); if (overlayState.lastTrigger && typeof overlayState.lastTrigger.focus === "function") { try { overlayState.lastTrigger.focus(); } catch (_err) {} } } } doc.addEventListener("keydown", function (event) { var active = overlayState.activeKey ? overlays[overlayState.activeKey] : null; if (!active || !active.container) return; if (event.key === "Escape") { event.preventDefault(); closeOverlay(active.key); return; } if (event.key === "Tab") { var panel = active.panel || active.container; var focusables = getFocusable(panel); if (!focusables.length) return; var first = focusables[0]; var last = focusables[focusables.length - 1]; if (event.shiftKey && doc.activeElement === first) { event.preventDefault(); last.focus(); } else if (!event.shiftKey && doc.activeElement === last) { event.preventDefault(); first.focus(); } } }); var checkoutEls = { form: q(hooks.donationForm), amountInput: q(hooks.amountInput), amountChips: qa(hooks.amountChip), summaryAmount: q(hooks.summaryAmount), hiddenTeamId: q(hooks.teamId), hiddenPlayerId: q('input[data-ff-player-id][name="player_id"]'), hiddenSponsorAmount: q('input[data-ff-sponsor-amount][name="sponsor_amount"]'), donorName: q(hooks.donorName), donorEmail: q(hooks.donorEmail), donorMessage: q(hooks.donorMessage), status: q(hooks.checkoutStatus), error: q(hooks.checkoutError), success: q(hooks.checkoutSuccess), stageForm: q('[data-ff-checkout-stage="form"]'), stageSuccess: q('[data-ff-checkout-stage="success"]'), stripeMount: q(hooks.stripeMount), paypalMount: q(hooks.paypalMount), stripeMessage: q("[data-ff-stripe-msg]"), stripeError: q("[data-ff-stripe-error]"), stripeSkeleton: q("[data-ff-stripe-skeleton]"), paypalMessage: q("[data-ff-paypal-msg]"), paypalError: q("[data-ff-paypal-error]"), paypalSkeleton: q("[data-ff-paypal-skeleton]") }; var sponsorEls = { modal: q(hooks.sponsorModal), form: q(hooks.sponsorForm), tierButtons: qa(hooks.sponsorTier), tierSelected: q(hooks.sponsorTierSelected), submit: q(hooks.sponsorSubmit), status: q(hooks.sponsorStatus), error: q(hooks.sponsorError), success: q(hooks.sponsorSuccess) }; var uiEls = { themeButtons: qa(hooks.themeToggle), backToTop: q(hooks.backToTop), tabs: q(hooks.tabs), floatingDonate: q(hooks.floatingDonate), burst: q(hooks.burst), sponsorWall: q(hooks.sponsorWall), sponsorWallEmpty: q(hooks.sponsorWallEmpty), hero: q("#home"), successMount: q("#ffDonationSuccessMount"), successTemplate: q("#ffDonationSuccessUpsellTemplate") }; function syncAmountUI(value) { var num = Math.max(0, Number(value || 0)); if (checkoutEls.summaryAmount) checkoutEls.summaryAmount.textContent = money(num); checkoutEls.amountChips.forEach(function (chip) { var chipValue = Number(chip.getAttribute("data-ff-amount") || 0); chip.setAttribute("aria-pressed", chipValue === num && num > 0 ? "true" : "false"); }); } function preloadCheckout(options) { options = options || {}; if (!checkoutEls.form) return; var amount = Number(options.amount || (checkoutEls.amountInput ? checkoutEls.amountInput.value : 0) || 0); var teamId = options.teamId || (checkoutEls.hiddenTeamId ? checkoutEls.hiddenTeamId.value : "") || "default"; var playerId = options.playerId || ""; var sponsorAmount = options.sponsorAmount || ""; if (checkoutEls.amountInput && amount > 0) { checkoutEls.amountInput.value = String(amount); } if (checkoutEls.hiddenTeamId) checkoutEls.hiddenTeamId.value = teamId; if (checkoutEls.hiddenPlayerId) checkoutEls.hiddenPlayerId.value = playerId; if (checkoutEls.hiddenSponsorAmount) { checkoutEls.hiddenSponsorAmount.value = sponsorAmount || (playerId && amount > 0 ? String(amount) : ""); } syncAmountUI(amount); debounceIntentRefresh(); } function setCheckoutStage(stage) { var isSuccess = stage === "success"; setHidden(checkoutEls.stageForm, isSuccess); setHidden(checkoutEls.stageSuccess, !isSuccess); if (checkoutEls.stageForm) checkoutEls.stageForm.dataset.state = isSuccess ? "hidden" : "visible"; if (checkoutEls.stageSuccess) checkoutEls.stageSuccess.dataset.state = isSuccess ? "visible" : "hidden"; } function validateCheckoutForm() { if (!checkoutEls.form || !checkoutEls.amountInput || !checkoutEls.donorName || !checkoutEls.donorEmail) { return false; } var amount = Number(checkoutEls.amountInput.value || 0); var valid = true; var firstInvalid = null; var checks = [ [checkoutEls.amountInput, amount >= 1], [checkoutEls.donorName, checkoutEls.donorName.value.trim().length >= 2], [checkoutEls.donorEmail, /\S+@\S+\.\S+/.test(checkoutEls.donorEmail.value.trim())] ]; checks.forEach(function (pair) { var field = pair[0]; var ok = pair[1]; if (!field) return; field.setAttribute("aria-invalid", ok ? "false" : "true"); if (!ok && !firstInvalid) firstInvalid = field; valid = valid && ok; }); if (!valid) { showMessage(checkoutEls.error, "Please review the amount, name, and email, then try again."); if (firstInvalid && typeof firstInvalid.focus === "function") firstInvalid.focus(); announce("Please review the amount, name, and email, then try again."); } else { showMessage(checkoutEls.error, ""); } return valid; } function getDonationPayload() { var payload = formDataObject(checkoutEls.form); payload.amount = Number(payload.amount || 0); payload.team_id = payload.team_id || "default"; payload.player_id = payload.player_id || ""; payload.sponsor_amount = payload.sponsor_amount || ""; return payload; } var paymentState = { stripe: null, elements: null, paymentElement: null, clientSecret: "", clientAmount: 0, stripeInitPromise: null, paypalInitPromise: null, paypalRendered: false }; function loadScript(src) { return new Promise(function (resolve, reject) { if (!src) { reject(new Error("Missing script URL")); return; } var existing = doc.querySelector('script[src="' + src + '"]'); if (existing) { if (existing.dataset.loaded === "true") { resolve(existing); return; } existing.addEventListener("load", function () { resolve(existing); }, { once: true }); existing.addEventListener("error", function () { reject(new Error("Failed to load " + src)); }, { once: true }); return; } var script = doc.createElement("script"); script.src = src; script.async = true; script.defer = true; script.addEventListener("load", function () { script.dataset.loaded = "true"; resolve(script); }, { once: true }); script.addEventListener("error", function () { reject(new Error("Failed to load " + src)); }, { once: true }); doc.head.appendChild(script); }); } async function ensureStripeLibrary() { if (!meta.stripePk || !meta.stripeJs) return null; if (paymentState.stripe) return paymentState.stripe; if (paymentState.stripeInitPromise) return paymentState.stripeInitPromise; paymentState.stripeInitPromise = (async function () { showMessage(checkoutEls.stripeMessage, "Preparing secure card entry…"); if (checkoutEls.stripeSkeleton) checkoutEls.stripeSkeleton.hidden = false; await loadScript(meta.stripeJs); if (!window.Stripe) { throw new Error("Stripe failed to load"); } paymentState.stripe = window.Stripe(meta.stripePk); showMessage(checkoutEls.stripeMessage, ""); if (checkoutEls.stripeSkeleton) checkoutEls.stripeSkeleton.hidden = true; return paymentState.stripe; })(); try { return await paymentState.stripeInitPromise; } catch (_err) { showMessage(checkoutEls.stripeMessage, ""); showMessage(checkoutEls.stripeError, "Stripe checkout is unavailable right now."); if (checkoutEls.stripeSkeleton) checkoutEls.stripeSkeleton.hidden = true; return null; } } async function createStripeIntent(payload) { if (!meta.stripeIntentEndpoint) return null; return fetchJSON(meta.stripeIntentEndpoint, { method: "POST", headers: defaultHeaders(true), body: JSON.stringify(payload), credentials: "same-origin" }); } function getClientSecret(data) { return ((data && data.clientSecret) || (data && data.client_secret) || (data && data.secret) || (data && data.paymentIntentClientSecret) || ""); } function buildStripeAppearance() { var dark = root.getAttribute("data-theme") === "dark"; return { theme: dark ? "night" : "stripe", variables: { colorPrimary: dark ? "#fb923c" : "#f97316", colorBackground: dark ? "#0f172a" : "#ffffff", colorText: dark ? "#f8fafc" : "#0f172a", colorDanger: "#dc2626", borderRadius: "16px" } }; } async function mountStripeForCurrentAmount(force) { if (!checkoutEls.amountInput || !checkoutEls.stripeMount || !meta.stripePk || !meta.stripeIntentEndpoint) { return; } var amount = Number(checkoutEls.amountInput.value || 0); if (amount < 1) return; var stripe = await ensureStripeLibrary(); if (!stripe) return; if (!force && paymentState.clientSecret && paymentState.clientAmount === amount && paymentState.paymentElement) { return; } showMessage(checkoutEls.stripeMessage, "Preparing secure card entry…"); showMessage(checkoutEls.stripeError, ""); if (checkoutEls.stripeSkeleton) checkoutEls.stripeSkeleton.hidden = false; try { var payload = getDonationPayload(); var data = await createStripeIntent(payload); var clientSecret = getClientSecret(data); if (!clientSecret) throw new Error("Missing payment client secret"); paymentState.clientSecret = clientSecret; paymentState.clientAmount = amount; if (paymentState.paymentElement) { try { paymentState.paymentElement.unmount(); } catch (_err) {} paymentState.paymentElement = null; } paymentState.elements = stripe.elements({ clientSecret: clientSecret, appearance: buildStripeAppearance() }); paymentState.paymentElement = paymentState.elements.create("payment"); checkoutEls.stripeMount.innerHTML = ""; paymentState.paymentElement.mount(checkoutEls.stripeMount); showMessage(checkoutEls.stripeMessage, ""); if (checkoutEls.stripeSkeleton) checkoutEls.stripeSkeleton.hidden = true; } catch (err) { showMessage(checkoutEls.stripeMessage, ""); showMessage(checkoutEls.stripeError, (err && err.message) || "Stripe checkout is unavailable right now."); if (checkoutEls.stripeSkeleton) checkoutEls.stripeSkeleton.hidden = true; } } async function ensurePayPal() { if (!meta.paypalClientId || !checkoutEls.paypalMount || !meta.paypalCreateEndpoint || !meta.paypalCaptureEndpoint) { return null; } if (window.paypal && window.paypal.Buttons) return window.paypal; if (paymentState.paypalInitPromise) return paymentState.paypalInitPromise; var sdkUrl = "https://www.paypal.com/sdk/js?client-id=" + encodeURIComponent(meta.paypalClientId) + "&currency=" + encodeURIComponent(meta.paypalCurrency) + "&intent=" + encodeURIComponent(meta.paypalIntent || "capture"); paymentState.paypalInitPromise = (async function () { showMessage(checkoutEls.paypalMessage, "Loading PayPal…"); if (checkoutEls.paypalSkeleton) checkoutEls.paypalSkeleton.hidden = false; await loadScript(sdkUrl); showMessage(checkoutEls.paypalMessage, ""); if (checkoutEls.paypalSkeleton) checkoutEls.paypalSkeleton.hidden = true; return window.paypal || null; })(); try { return await paymentState.paypalInitPromise; } catch (_err) { showMessage(checkoutEls.paypalMessage, ""); showMessage(checkoutEls.paypalError, "PayPal checkout is unavailable right now."); if (checkoutEls.paypalSkeleton) checkoutEls.paypalSkeleton.hidden = true; return null; } }

async function renderPayPalButtons() { if (paymentState.paypalRendered || !checkoutEls.paypalMount) return; var paypal = await ensurePayPal(); if (!paypal || !paypal.Buttons) return; checkoutEls.paypalMount.innerHTML = ""; paypal.Buttons({ style: { shape: "pill", height: 44, layout: "vertical", color: "gold" }, createOrder: async function () { if (!validateCheckoutForm()) { throw new Error("Please review the amount, name, and email, then try again."); } var data = await fetchJSON(meta.paypalCreateEndpoint, { method: "POST", headers: defaultHeaders(true), body: JSON.stringify(getDonationPayload()), credentials: "same-origin" }); return data.orderID || data.orderId || data.id; }, onApprove: async function (data) { showMessage(checkoutEls.status, "Finalizing PayPal payment…"); await fetchJSON(meta.paypalCaptureEndpoint, { method: "POST", headers: defaultHeaders(true), body: JSON.stringify({ orderID: data.orderID, orderId: data.orderID, payload: getDonationPayload() }), credentials: "same-origin" }); onDonationSuccess(); }, onError: function (err) { showMessage(checkoutEls.status, ""); showMessage(checkoutEls.paypalError, (err && err.message) || "PayPal checkout is unavailable right now."); } }).render(checkoutEls.paypalMount); paymentState.paypalRendered = true; } var intentRefreshTimer = null; function debounceIntentRefresh() { if (intentRefreshTimer) window.clearTimeout(intentRefreshTimer); intentRefreshTimer = window.setTimeout(function () { mountStripeForCurrentAmount(false); }, 550); } async function handleCheckoutSubmit(event) { event.preventDefault(); if (!validateCheckoutForm()) return; showMessage(checkoutEls.status, "Preparing secure payment…"); showMessage(checkoutEls.error, ""); var amount = Number((checkoutEls.amountInput && checkoutEls.amountInput.value) || 0); syncAmountUI(amount); if (meta.stripePk && meta.stripeIntentEndpoint) { await mountStripeForCurrentAmount(true); if (!paymentState.stripe || !paymentState.elements) { showMessage(checkoutEls.status, ""); showMessage(checkoutEls.error, "A secure payment method could not be loaded. Please try again."); return; } var result = await paymentState.stripe.confirmPayment({ elements: paymentState.elements, confirmParams: { return_url: meta.stripeReturnUrl || window.location.href, receipt_email: checkoutEls.donorEmail ? checkoutEls.donorEmail.value.trim() : undefined }, redirect: "if_required" }); if (result && result.error) { showMessage(checkoutEls.status, ""); showMessage(checkoutEls.error, result.error.message || "Payment could not be completed."); return; } var status = result && result.paymentIntent ? result.paymentIntent.status : ""; if (status === "succeeded" || status === "processing" || status === "requires_capture") { onDonationSuccess(); return; } } if (meta.paypalClientId && meta.paypalCreateEndpoint) { showMessage(checkoutEls.status, "Use PayPal below or card entry above to complete support."); return; } showMessage(checkoutEls.status, ""); showMessage(checkoutEls.error, "A secure payment method could not be loaded. Please try again."); } function createBurst() { var host = uiEls.burst; if (!host) return; host.innerHTML = ""; var rect = { x: window.innerWidth / 2, y: window.innerHeight / 2 }; for (var i = 0; i < 18; i += 1) { var dot = doc.createElement("span"); var angle = (Math.PI * 2 * i) / 18; var distance = 70 + Math.random() * 90; dot.className = "ff-burst__dot"; dot.style.left = rect.x + "px"; dot.style.top = rect.y + "px"; dot.style.setProperty("--x", Math.cos(angle) * distance + "px"); dot.style.setProperty("--y", Math.sin(angle) * distance + "px"); host.appendChild(dot); } window.setTimeout(function () { host.innerHTML = ""; }, 1100); } function cloneSuccessUpsell() { if (!uiEls.successMount || !uiEls.successTemplate) return; if (uiEls.successMount.querySelector("[data-ff-success-upsell]")) return; if (String(uiEls.successTemplate.tagName).toUpperCase() !== "TEMPLATE") return; uiEls.successMount.hidden = false; uiEls.successMount.appendChild(uiEls.successTemplate.content.cloneNode(true)); } function onDonationSuccess() { showMessage(checkoutEls.status, ""); showMessage(checkoutEls.error, ""); setCheckoutStage("success"); cloneSuccessUpsell(); createBurst(); toast("Support completed. Your receipt should arrive by email shortly."); announce("Support completed. Your receipt should arrive by email shortly."); } function hydrateSuccessFromUrl() { var params = new URLSearchParams(window.location.search); var redirectStatus = params.get("redirect_status"); var paymentIntent = params.get("payment_intent"); var paypalSuccess = params.get("paypal") || params.get("paypal_status") || params.get("status"); if (redirectStatus === "succeeded" || paymentIntent || paypalSuccess === "success") { setCheckoutStage("success"); cloneSuccessUpsell(); openOverlay("checkout"); } } function handleShare(event) { if (event) event.preventDefault(); var title = doc.title || "FutureFunded fundraiser"; var text = "Support this fundraiser"; var url = meta.shareUrl || window.location.href; if (navigator.share) { navigator.share({ title: title, text: text, url: url }).catch(function () {}); return; } copyText(url) .then(function () { toast("Fundraiser link copied."); }) .catch(function () { toast("Share link ready: " + url); }); } function syncThemeUI(theme) { var nextTheme = theme === "dark" ? "dark" : "light"; root.setAttribute("data-theme", nextTheme); root.style.colorScheme = nextTheme; if (body) body.setAttribute("data-ff-theme", nextTheme); uiEls.themeButtons.forEach(function (btn) { btn.setAttribute("aria-pressed", nextTheme === "dark" ? "true" : "false"); btn.textContent = nextTheme === "dark" ? "Light mode" : "Theme"; btn.setAttribute("aria-label", nextTheme === "dark" ? "Switch to light theme" : "Switch to dark theme"); }); if (paymentState.clientSecret) { mountStripeForCurrentAmount(true); } } function initTheme() { var stored = ""; try { stored = window.localStorage.getItem("ff-theme") || window.localStorage.getItem("ff.theme") || window.localStorage.getItem("ff_theme") || ""; } catch (_err) {} if (stored === "dark" || stored === "light") { syncThemeUI(stored); return; } var current = root.getAttribute("data-theme") === "dark" ? "dark" : "light"; syncThemeUI(current); } function updateBackToTop() { if (!uiEls.backToTop) return; var visible = window.scrollY > 640; uiEls.backToTop.classList.toggle("is-visible", visible); } function updateFloatingDonate() { if (!uiEls.floatingDonate) return; var show = window.innerWidth < 1024 && window.scrollY > Math.max(((uiEls.hero && uiEls.hero.offsetHeight) || 560) * 0.7, 420) && overlayState.activeKey !== "checkout"; uiEls.floatingDonate.classList.toggle("is-visible", show); uiEls.floatingDonate.hidden = !show; } function initScrollSpy() { var links = qa('[data-ff-scrollspy] a[href^="#"], .ff-tabs a[href^="#"], .ff-nav a[href^="#"], .ff-footer a[href^="#"]'); var map = []; links.forEach(function (link) { var href = link.getAttribute("href") || ""; if (!href || href.charAt(0) !== "#") return; var id = href.slice(1); if (!id) return; var section = q("#" + escapeId(id)); map.push({ link: link, section: section }); }); function refresh() { var currentId = "home"; var bestTop = Number.NEGATIVE_INFINITY; map.forEach(function (item) { if (!item.section) return; var rect = item.section.getBoundingClientRect(); if (rect.top <= 140 && rect.top > bestTop) { bestTop = rect.top; currentId = item.section.id; } }); map.forEach(function (item) { var active = item.section && item.section.id === currentId; if (active) item.link.setAttribute("aria-current", "location"); else item.link.removeAttribute("aria-current"); }); } refresh(); window.addEventListener("scroll", refresh, { passive: true }); window.addEventListener("resize", refresh); } function populateSponsorWall() { var wall = uiEls.sponsorWall; var empty = uiEls.sponsorWallEmpty; if (!wall) return; var sources = [] .concat(Array.isArray(ffConfig && ffConfig.sponsors) ? ffConfig.sponsors : []) .concat(Array.isArray(ffConfig && ffConfig.sponsor_wall) ? ffConfig.sponsor_wall : []) .concat(Array.isArray(ffConfig && ffConfig.sponsorWall) ? ffConfig.sponsorWall : []); var seen = {}; var sponsors = sources.filter(function (item) { var name = ((item && (item.name || item.business_name || item.title)) || "").trim(); var key = name.toLowerCase(); if (!name || seen[key]) return false; seen[key] = true; return true; }).slice(0, 12); if (!sponsors.length) { setHidden(wall, true); if (empty) empty.hidden = false; return; } wall.innerHTML = ""; sponsors.forEach(function (item) { var card = doc.createElement("article"); var tier = item.tier || item.level || item.package || "Sponsor"; var name = item.name || item.business_name || item.title || "Community partner"; var note = item.note || item.description || "Community support partner"; card.className = "ff-sponsorWallCard"; card.innerHTML = [ '<div class="ff-sponsorWallCard__tier">' + escapeHTML(tier) + "</div>", '<div class="ff-sponsorWallCard__name">' + escapeHTML(name) + "</div>", '<div class="ff-help ff-muted">' + escapeHTML(note) + "</div>" ].join(""); wall.appendChild(card); }); setHidden(wall, false); if (empty) empty.hidden = true; } function selectSponsorTier(value) { if (sponsorEls.tierSelected) sponsorEls.tierSelected.value = value || ""; sponsorEls.tierButtons.forEach(function (button) { var active = button.getAttribute("data-ff-sponsor-tier") === value; button.setAttribute("aria-pressed", active ? "true" : "false"); }); } function validateSponsorForm() { if (!sponsorEls.form) return false; var fields = [ q('input[name="sponsor_name"]', sponsorEls.form), q('input[name="contact_name"]', sponsorEls.form), q('input[name="contact_email"]', sponsorEls.form) ]; var checks = [ fields[0] ? fields[0].value.trim().length >= 2 : true, fields[1] ? fields[1].value.trim().length >= 2 : true, fields[2] ? /\S+@\S+\.\S+/.test(fields[2].value.trim()) : true ]; var valid = true; var firstInvalid = null; fields.forEach(function (field, index) { if (!field) return; var ok = checks[index]; field.setAttribute("aria-invalid", ok ? "false" : "true"); if (!ok && !firstInvalid) firstInvalid = field; valid = valid && ok; }); if (!valid) { showMessage(sponsorEls.error, "Please add your name, contact name, and a valid email."); showMessage(sponsorEls.status, ""); if (firstInvalid && typeof firstInvalid.focus === "function") firstInvalid.focus(); return false; } showMessage(sponsorEls.error, ""); return true; } function buildSponsorPayload() { var payload = formDataObject(sponsorEls.form); payload.page_url = payload.page_url || window.location.href; payload.share_url = payload.share_url || meta.shareUrl || window.location.href; payload.sponsor_tier = payload.sponsor_tier || (sponsorEls.tierSelected ? sponsorEls.tierSelected.value : "") || ""; return payload; } function getSponsorEndpoint() { if (!sponsorEls.form) return ""; return (sponsorEls.form.getAttribute("action") || sponsorEls.form.getAttribute("data-ff-endpoint") || sponsorEls.form.getAttribute("data-endpoint") || (sponsorEls.modal && sponsorEls.modal.getAttribute("data-ff-endpoint")) || "").trim(); } async function submitSponsorForm(event) { if (event) event.preventDefault(); if (!sponsorEls.form) return; if (!validateSponsorForm()) return; showMessage(sponsorEls.status, "Sending sponsor inquiry…"); showMessage(sponsorEls.error, ""); showMessage(sponsorEls.success, ""); var endpoint = getSponsorEndpoint(); var payload = buildSponsorPayload(); if (!endpoint) { showMessage(sponsorEls.status, ""); showMessage(sponsorEls.success, meta.sponsorContact ? "Sponsor details are ready. Email " + meta.sponsorContact + " to finish the intro." : "Sponsor details are ready. Add a sponsor endpoint or contact email to submit automatically."); try { await copyText(JSON.stringify(payload, null, 2)); toast("Sponsor brief copied."); } catch (_err) {} return; } try { var data = await fetchJSON(endpoint, { method: "POST", headers: defaultHeaders(true), body: JSON.stringify(payload), credentials: "same-origin" }); showMessage(sponsorEls.status, ""); showMessage(sponsorEls.success, data.message || "Sponsor inquiry received. We’ll follow up with next steps."); toast("Sponsor inquiry received."); sponsorEls.form.reset(); selectSponsorTier(""); } catch (err) { showMessage(sponsorEls.status, ""); showMessage(sponsorEls.error, (err && err.message) || "Unable to send sponsor inquiry right now."); } } function initVideoOpen(trigger) { var data = readTriggerData(trigger); var title = data.videoTitle || (trigger ? trigger.getAttribute("aria-label") : "") || "Watch the story"; var src = data.videoSrc || ""; var titleNode = q("[data-ff-video-title]"); var statusNode = q(hooks.videoStatus); if (titleNode) titleNode.textContent = title; if (statusNode) statusNode.textContent = src ? "Loading on open." : "Video source unavailable."; if (src) { window.setTimeout(function () { loadVideo(src, title); }, 40); } } function loadVideo(src, title) { var mount = q(hooks.videoMount); var statusNode = q(hooks.videoStatus); if (!mount || !src) return; mount.innerHTML = ""; var iframe = doc.createElement("iframe"); iframe.src = src; iframe.title = title || "Program video"; iframe.setAttribute("allowfullscreen", ""); iframe.setAttribute("loading", "lazy"); iframe.setAttribute("allow", "accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"); mount.appendChild(iframe); if (statusNode) statusNode.textContent = "Video ready."; } function unloadVideo() { var mount = q(hooks.videoMount); var statusNode = q(hooks.videoStatus); if (mount) mount.innerHTML = ""; if (statusNode) statusNode.textContent = "Ready when you are."; } function initHashOpen() { var hash = (window.location.hash || "").replace(/^#/, ""); if (!hash) return; if (hash === "checkout") { openOverlay("checkout"); mountStripeForCurrentAmount(false); renderPayPalButtons(); } else if (hash === "sponsor-interest") { openOverlay("sponsor"); } else if (hash === "press-video") { openOverlay("video"); } else if (hash === "terms") { openOverlay("terms"); } else if (hash === "privacy") { openOverlay("privacy"); } } doc.addEventListener("click", function (event) { var target = event.target; if (!target || target.nodeType !== 1) return; var openCheckout = target.closest(hooks.openCheckout || ""); if (openCheckout) { event.preventDefault(); preloadCheckout(readTriggerData(openCheckout)); setCheckoutStage("form"); openOverlay("checkout", openCheckout); if (checkoutEls.amountInput && !checkoutEls.amountInput.value) checkoutEls.amountInput.focus(); mountStripeForCurrentAmount(false); renderPayPalButtons(); return; } var openDrawer = target.closest(hooks.openDrawer || ""); if (openDrawer) { event.preventDefault(); openOverlay("drawer", openDrawer); return; } var openSponsor = target.closest(hooks.openSponsor || ""); if (openSponsor) { event.preventDefault(); var sponsorData = readTriggerData(openSponsor); if (sponsorData.sponsorTier) selectSponsorTier(sponsorData.sponsorTier); openOverlay("sponsor", openSponsor); return; } var openVideo = target.closest(hooks.openVideo || ""); if (openVideo) { event.preventDefault(); openOverlay("video", openVideo); initVideoOpen(openVideo); return; } var openTerms = target.closest(hooks.openTerms || ""); if (openTerms) { event.preventDefault(); openOverlay("terms", openTerms); return; } var openPrivacy = target.closest(hooks.openPrivacy || ""); if (openPrivacy) { event.preventDefault(); openOverlay("privacy", openPrivacy); return; } var closeCheckout = target.closest(hooks.closeCheckout || ""); if (closeCheckout) { event.preventDefault(); closeOverlay("checkout"); return; } var closeDrawer = target.closest(hooks.closeDrawer || ""); if (closeDrawer) { event.preventDefault(); closeOverlay("drawer"); return; } var closeSponsor = target.closest(hooks.closeSponsor || ""); if (closeSponsor) { event.preventDefault(); closeOverlay("sponsor"); return; } var closeVideo = target.closest(hooks.closeVideo || ""); if (closeVideo) { event.preventDefault(); closeOverlay("video"); return; } var closeTerms = target.closest(hooks.closeTerms || ""); if (closeTerms) { event.preventDefault(); closeOverlay("terms"); return; } var closePrivacy = target.closest(hooks.closePrivacy || ""); if (closePrivacy) { event.preventDefault(); closeOverlay("privacy"); return; } var share = target.closest(hooks.share || ""); if (share) { handleShare(event); return; } var amountChip = target.closest(hooks.amountChip || ""); if (amountChip) { event.preventDefault(); var amount = Number(amountChip.getAttribute("data-ff-amount") || 0); preloadCheckout({ amount: amount }); if (overlayState.activeKey !== "checkout") { setCheckoutStage("form"); openOverlay("checkout", amountChip); } mountStripeForCurrentAmount(false); renderPayPalButtons(); return; } var sponsorTier = target.closest(hooks.sponsorTier || ""); if (sponsorTier) { event.preventDefault(); selectSponsorTier(sponsorTier.getAttribute("data-ff-sponsor-tier") || ""); return; } var successShare = target.closest("[data-ff-success-share]"); if (successShare) { handleShare(event); return; } var successCopy = target.closest("[data-ff-success-copy]"); if (successCopy) { event.preventDefault(); copyText(meta.shareUrl) .then(function () { toast("Fundraiser link copied."); }) .catch(function () { toast("Unable to copy the fundraiser link."); }); } }); if (checkoutEls.amountInput) { checkoutEls.amountInput.addEventListener("input", function () { syncAmountUI(checkoutEls.amountInput.value); debounceIntentRefresh(); }); } if (checkoutEls.form) { checkoutEls.form.addEventListener("submit", handleCheckoutSubmit); } if (sponsorEls.tierButtons.length) { selectSponsorTier(""); } if (sponsorEls.form) { sponsorEls.form.addEventListener("submit", submitSponsorForm); } if (uiEls.backToTop) { uiEls.backToTop.addEventListener("click", function (event) { event.preventDefault(); window.scrollTo({ top: 0, behavior: "smooth" }); }); } window.addEventListener("scroll", function () { updateBackToTop(); updateFloatingDonate(); }, { passive: true }); window.addEventListener("resize", updateFloatingDonate); document.addEventListener("ff:theme-changed", function (event) { var theme = event && event.detail ? event.detail.theme : root.getAttribute("data-theme"); syncThemeUI(theme); }); initTheme(); syncAmountUI(checkoutEls.amountInput ? checkoutEls.amountInput.value : 0); updateBackToTop(); updateFloatingDonate(); initScrollSpy(); populateSponsorWall(); hydrateSuccessFromUrl(); initHashOpen(); if (overlays.checkout.container && overlays.checkout.container.hasAttribute("data-ff-stripe-preload")) { mountStripeForCurrentAmount(false); renderPayPalButtons(); } })(); /* FF_THEME_CONTROLLER_V3_START */ (function () { if (window.__FF_THEME_CONTROLLER_V3__) return; window.__FF_THEME_CONTROLLER_V3__ = true; var root = document.documentElement; var KEY = "ff.theme"; var LEGACY_KEYS = ["ff_theme", "futurefunded.theme", "ff-theme"]; var SELECTOR = "[data-ff-theme-toggle], .ff-themeToggle, #ff-theme-toggle"; var media = window.matchMedia ? window.matchMedia("(prefers-color-scheme: dark)") : null; function readStoredTheme() { try { var direct = localStorage.getItem(KEY); if (direct === "light" || direct === "dark") return direct; for (var i = 0; i < LEGACY_KEYS.length; i += 1) { var legacy = localStorage.getItem(LEGACY_KEYS[i]); if (legacy === "light" || legacy === "dark") return legacy; } } catch (_err) {} return null; } function writeStoredTheme(theme) { try { localStorage.setItem(KEY, theme); for (var i = 0; i < LEGACY_KEYS.length; i += 1) { localStorage.setItem(LEGACY_KEYS[i], theme); } } catch (_err) {} } function getCurrentTheme() { var attr = root.getAttribute("data-theme"); if (attr === "light" || attr === "dark") return attr; if (document.body) { var bodyAttr = document.body.getAttribute("data-ff-theme"); if (bodyAttr === "light" || bodyAttr === "dark") return bodyAttr; } var stored = readStoredTheme(); if (stored) return stored; if (media && media.matches) return "dark"; return "light"; } function allToggles() { return Array.prototype.slice.call(document.querySelectorAll(SELECTOR)); } function paintTheme(theme, persist) { var resolved = theme === "dark" ? "dark" : "light"; root.setAttribute("data-theme", resolved); root.style.colorScheme = resolved; if (document.body) { document.body.setAttribute("data-ff-theme", resolved); } var toggles = allToggles(); for (var i = 0; i < toggles.length; i += 1) { toggles[i].setAttribute("aria-pressed", resolved === "dark" ? "true" : "false"); toggles[i].setAttribute("aria-label", resolved === "dark" ? "Switch to light theme" : "Switch to dark theme"); toggles[i].textContent = resolved === "dark" ? "Light mode" : "Theme"; } if (persist) writeStoredTheme(resolved); try { document.dispatchEvent(new CustomEvent("ff:theme-changed", { detail: { theme: resolved } })); } catch (_err) {} } function toggleTheme() { paintTheme(getCurrentTheme() === "dark" ? "light" : "dark", true); } document.addEventListener("click", function (event) { var target = event.target; if (!target || !target.closest) return; var toggle = target.closest(SELECTOR); if (!toggle) return; event.preventDefault(); if (event.stopImmediatePropagation) event.stopImmediatePropagation(); event.stopPropagation(); toggleTheme(); }, true); document.addEventListener("keydown", function (event) { var target = event.target; if (!target || !target.closest) return; var toggle = target.closest(SELECTOR); if (!toggle) return; if (event.key !== "Enter" && event.key !== " ") return; event.preventDefault(); if (event.stopImmediatePropagation) event.stopImmediatePropagation(); event.stopPropagation(); toggleTheme(); }, true); if (media && media.addEventListener) { media.addEventListener("change", function (event) { var stored = readStoredTheme(); if (stored === "light" || stored === "dark") return; paintTheme(event.matches ? "dark" : "light", false); }); } paintTheme(getCurrentTheme(), false); })(); /* FF_THEME_CONTROLLER_V3_END */

